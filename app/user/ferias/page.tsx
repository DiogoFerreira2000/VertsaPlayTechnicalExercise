import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Marcação de Férias',
};

export default function AdminPage() {
    return (
      <div>
        <h1>Página das férias</h1>
      </div>
    );
}